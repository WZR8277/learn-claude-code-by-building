# s04 Reflection

我个人认为 Hooks 这个机制本身没有特别神秘的地方，它更像 Java 编程里的 AOP：围绕一些切面或生命周期点做额外处理。

这章真正有价值的地方不在于 Hook 这个名字，而在于它把原本可能散落在 Agent Loop 里的横切逻辑抽出来。比如权限检查、工具调用日志、工具执行后的观察、停止前的总结，都可以围绕固定切点注册，而不是继续把主循环改得越来越重。

所以我对 s04 的理解是：Hooks 是一种扩展组织方式。它不改变 Agent Loop 的核心协议，也不直接带来新的智能能力，但它让后续机制更容易挂到合适位置，同时保持主循环的结构更清晰。
